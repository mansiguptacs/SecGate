# Stub for real Pomerium — not used by the Phase 2 policy shim.
# When IdP is ready, enable runtime_flags.mcp and map policy.yaml rules here.

authenticate_service_url: https://authenticate.localhost.pomerium.io
databroker_storage_type: memory

runtime_flags:
  mcp: true

routes:
  - from: https://mcp.localhost.pomerium.io
    to: http://host.docker.internal:3100
    name: secgate-infra-mcp
    allow_websockets: true
    # Policy: see policy.yaml (PPL-shaped). Map allow_tools / guardian_only_tools
    # onto Pomerium MCP tool policies when swapping the shim.
